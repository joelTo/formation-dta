CREATE TABLE `pizza` (
  `id` int(11) NOT NULL,
  `libelle` varchar(50) NOT NULL,
  `reference` varchar(50) NOT NULL,
  `prix` double NOT NULL,
  `url` varchar(5000) NOT NULL,
  `catPizza` varchar(15) NOT NULL
);
